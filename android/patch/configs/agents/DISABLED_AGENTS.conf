# 禁用的 Agent 列表
# 这些是 miclaw 特有/不适合的 Agent，修改其 config.json enabled=false
#
# 文件: assets/agents/<agent_id>/config.json

DISABLED_AGENTS=(
  "osbot.nsfw"               # 不适合
  "osbot.qiushi"             # 小米内网
  "osbot.trump"              # 娱乐
  "osbot.zhangxuefeng"       # 娱乐
  "osbot.feedback"           # 连小米服务器
  "osbot.account_cloud"      # 小米账号
)

# 保留 (enabled=true, 不改):
# osbot.main            — 主助手
# osbot.chat            — 聊天
# osbot.calendar        — 日历
# osbot.call_agent      — 电话
# osbot.communication_assistant — 通讯
# osbot.content_assistant       — 内容
# osbot.device_interconnect     — 设备互联
# osbot.entertainment_assistant — 娱乐
# osbot.imaging_assistant       — 影像
# osbot.os_helper               — 系统助手
# osbot.overlayassistant        — 悬浮助手 (fallback)
# osbot.web_explore             — 网页探索
# osbot.xiaomihome              — 智能家居
# osbot.worker                  — 工作
