apiVersion: v1
kind: Service
metadata:
  name: mbclaw-lite
  namespace: mbclaw
  labels:
    app: mbclaw-lite
spec:
  type: ClusterIP
  selector:
    app: mbclaw-lite
  ports:
  - port: 8011
    targetPort: 8011
    protocol: TCP
    name: http
